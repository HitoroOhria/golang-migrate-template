version: "3"

includes:
  compose:
    taskfile: ./taskfiles/TaskfileCompose.yml
  mysql:
    taskfile: ./taskfiles/TaskfileMySQL.yml

tasks:
  default:
    desc: List all available tasks
    cmds:
      - task -l --sort none
